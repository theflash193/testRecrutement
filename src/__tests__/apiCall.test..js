const  apiCall = require("../utils/apiCall");

describe("test apiCall", () => { 

    test('should return ', () => {
        const offers = [];
        expect(apiCall.apiCall()).toBe(42);
    });

 });
